---
id: rust-migration-lock-20260904
intake_id: rust-migration-intake-20260904
type: plan
lane: high-risk
status: active
created: 2026-09-04
updated: 2026-09-04
---

# Plan: Go→Rust big-bang migration

## Outcome
- result: `zbrain` is rewritten from Go (32.3k LOC, 331 tests) to Rust as a big-bang migration on a long-lived `rust-rewrite` branch. At cutover, a single PR replaces the Go tree, CI, Makefile, and docs authority with a Rust workspace whose behavior is verified identical to the frozen Go binary via a living-oracle parity strategy.
- success_signals:
  - Golden-fixture parity: Rust output is byte-identical to the Go oracle on deterministic fields (ask/doctor/status JSON, digests, claim/evidence file trees, source.yaml).
  - Differential CLI harness runs both binaries against the same isolated `ZBRAIN_HOME` fixture set and diffs schema-level JSON; green for every command surface before cutover.
  - 1:1 test port for trust-critical modules: lifecycle, trust_validation, challenge/approval, evidence immutability, workspace isolation.
  - p95@100K ask latency ≤ Go baseline (`docs/proofs/bench-baseline-full.json`).
  - FTS5 confirmed available in the bundled rusqlite build (compile-time assert + smoke).
  - Full Rust gate green at cutover: `cargo test --workspace`, `cargo clippy -- -D warnings`, `cargo build --release`, `make smoke` (isolated ZBRAIN_HOME, rc=0), `git diff --check`.
  - `git revert` of the cutover commit restores the Go tree and gate intact.

## Authority and Requirements
- authority:
  - Owner decision 2026-09-04: migrate Go→Rust, big-bang strategy, drivers = perf + single static binary + owning a Rust codebase + embeddability ("tất cả").
  - Owner decision 2026-09-04: the AGENTS.md "Go-native CLI" constraint is superseded at cutover (M8); it remains in force on `master` until the cutover PR merges.
  - `trusted-memory-spec.md` and `docs/trusted-agent-gateway-spec.md` define behavior contracts that do not change with the language.
  - Exploration evidence 2026-09-04: embedder is a pure in-process token embedder (no HTTP sidecar, `internal/runtime/embedder.go`); concurrency surface is 3 files only (coordination, embedder, mcp safeWriter); MCP tests drive in-memory transports; workspace flock is plain `flock(2)` (`internal/runtime/coordination.go`).
- requirements:
  - R1 [accepted]: Cutover is one PR that deletes the Go tree and switches CI to the Rust toolchain (`cargo test --workspace`, `cargo clippy -D warnings`, `cargo build --release`, `make smoke`, `CGO` concept removed); `git diff --check` stays clean. | source: owner big-bang decision
  - R2 [accepted]: Data formats are frozen bit-for-bit: OKF Markdown claims, evidence `raw`+`source.yaml` (0400), index SQLite schema, file modes (dirs 0700, canonical md 0600, evidence 0400, derived 0600). A Go binary must read a Rust-written tree and vice versa. | source: trusted-memory-spec.md §7-§9
  - R3 [accepted]: Golden fixtures generated by the Go oracle from a shared fixture set gate every milestone that touches canonical or trusted-output behavior. | source: big-bang risk mitigation
  - R4 [accepted]: Differential CLI harness compares Go vs Rust binary output on the same `ZBRAIN_HOME` fixture; runs as a CI job on `rust-rewrite` from M2 onward. | source: drift risk mitigation
  - R5 [accepted]: SQLite stack is `rusqlite` with `bundled` feature; first M4 task asserts FTS5 availability at compile/run time; p95@100K bench must be ≤ Go baseline. | source: owner stack choice; perf driver
  - R6 [accepted]: MCP gateway is hand-rolled JSON-RPC over stdio (serde_json), preserving the current protocol revisions and behaviors: legacy handshake, stateless `server/discover`, per-request `_meta`, `isError` for schema-invalid input, `-32602` oversized/unknown, `-32603` server faults; testable over in-memory transports via IO traits. | source: internal/mcp current behavior; rmcp rejected (custom protocol surface)
  - R7 [accepted]: 1:1 test port is mandatory for trust-critical modules (lifecycle, trust_validation, challenge/approval, evidence immutability, workspace isolation); non-trust surfaces may port as invariants + smoke. | source: owner "chắc chắn" requirement, effort balance
  - R8 [accepted]: Dependency budget ≤ 10 crates total (serde, serde_json, serde_yml, rusqlite, include_dir, console, chrono, libc + transitive); no async runtime (no tokio); view server and embedder port with std-only I/O. | source: exploration facts
  - R9 [accepted]: `master` stays Go and frozen for features until cutover; all migration PRs target the `rust-rewrite` branch; critical Go bugfixes may be cherry-picked to both sides until M8. | source: owner strategy choice
  - R10 [accepted]: AGENTS.md, `.github/workflows/test.yml`, `Makefile`, README/spec build-test sections are rewritten to Rust authority inside the cutover PR, not before. | source: owner authority decision

## Non-goals
- NG1: Behavior redesign or new features during migration; port behavior as-is (refactor only where Go cannot translate).
- NG2: Remote/HTTP MCP transport, REST, connectors, network fetch — unchanged non-goals of the product.
- NG3: Data format changes, schema migrations, or new file layouts.
- NG4: Windows cross-compile targets; CI stays ubuntu + macOS.
- NG5: tokio/async anywhere in the workspace.
- NG6: Deleting or degrading the Go tree before the cutover gate passes.
- NG7: Replacing the token-loopback embedding algorithm with a different model.

## Approach and Risks
- approach: Cargo workspace `crates/zbrain` (bin crate; internal modules mirror `internal/runtime` package split) on branch `rust-rewrite`. The Go tree stays on `master` as a frozen living oracle: a fixture generator (`crates/tools/fixture-gen`, a small Go program reusing `internal/runtime`) renders deterministic fixtures (ask/doctor/status JSON, digests, file trees, source.yaml) from a shared fixture workspace into `crates/zbrain/tests/fixtures/`; every phase gates on those golden files plus a differential CLI harness (`scripts/parity.sh` runs Go and Rust binaries against the same isolated `ZBRAIN_HOME` and diffs schema-level JSON with tolerant timestamps/IDs). Phase order follows real dependency: foundation (paths/perms/flock) → assets/setup → claims+evidence → lifecycle+trust+lint → index+query+embed → MCP → approval/campaign → CLI/view/eval → cutover. Dependencies ≤ 10 crates: serde, serde_json, serde_yml, rusqlite (bundled), include_dir, console, chrono, libc. No async runtime; std threads only (view server, safeWriter mutex).
- constraints:
  - Behavior ported as-is; no redesign, no format changes (R2).
  - `master` untouched except Go-side critical fixes; all work on `rust-rewrite` (R9).
  - Fixture generation is Go-oracle-owned; Rust tests must not hand-maintain parallel expectations.
  - Story/phase definitions below are immutable once written.
- risks:
  - FTS5 not enabled in libsqlite3-sys `bundled` build. Mitigation: first M4 task is a compile+run assertion; fallback path is enabling `SQLITE_ENABLE_FTS5` via the bundled feature's compile opts or vendored build config. If neither works, phase stops (premise collapse — index behavior is the product).
  - Hand-rolled MCP drifts from real clients. Mitigation: port the full MCP conformance test set (mcp_test.go, campaign_test.go transports) and run a real Claude/Codex stdio smoke before M6 sign-off.
  - Oracle drift over 6–8 weeks (Go frozen but critical fixes may cherry-pick). Mitigation: differential harness runs in CI on every `rust-rewrite` PR; fixture regeneration is part of any Go-side cherry-pick.
  - Ownership-model rewrites balloon scope. Mitigation: translate data flow first (owned values read from disk); `Arc/RwLock` only where the Go mutex surface is (coordination, safeWriter, embedder).
  - Golden-fixture byte drift (YAML emitter ordering, float formatting). Mitigation: fixture comparison normalizes only timestamps/IDs; everything else byte-compared; serde_yml two-column block style configured to match yaml.v3 output order.
  - Cutover breaks consumers (skills, engine assets referencing Go commands). Mitigation: M8 includes a repo-wide grep for Go-specific references; assets are language-neutral Markdown and only command names/flags change (none planned).
  - Recovery: any phase stops on failed gate without rewriting canonical surfaces; the only external-state change is the cutover commit, reverted by `git revert` (tags kept for the last Go release).
- rejected_alternatives:
  - Strangler per-subsystem coexistence: two live binaries to maintain for months; rejected by owner (big-bang chosen).
  - Rust FFI sidecar keeping Go CLI: keeps Go forever; contradicts owner driver (own a Rust codebase).
  - `rmcp` official SDK: its model of the protocol fights the custom revisions (`server/discover`, per-request `_meta`, exact error-code semantics); hand-rolled serde_json is smaller and testable.
  - Turso/Limbo pure-Rust SQLite: not production-ready, FTS5 incomplete; rusqlite bundled matches the modernc engine bit-for-bit.
  - tokio: concurrency surface is 3 files; async adds runtime risk with zero payoff.
  - New separate repo: loses asset/history adjacency; in-repo `crates/` keeps one identity.

## Phases and Verification
<!-- Phase and task definitions are immutable after to-plan. Do not add task status fields. Append-only Progress is the sole task execution-status source. Only each phase lifecycle status changes to mirror DB transitions: to-plan=planned; work after run create=in-progress; clean durable check=checked; closing handoff=done. Each planned phase records phase_slug, story_id, status, goal, depends_on, waves, tasks, and checks. -->
- planning_status: planned
- phases:
  - phase_slug: m0-foundation
    story_id: rust-m0-foundation-20260904
    status: planned
    goal: Rust workspace scaffold with paths, file modes, config, workspace, and flock ported
    depends_on: none
    requirements: [R2, R8, R9]
    allowed_surfaces: [Cargo.toml, crates/zbrain/, crates/tools/, .gitignore, scripts/]
    avoided_surfaces: [internal/, cmd/, Makefile, .github/]
    waves:
      - wave: W1
        goal: Scaffold and boot the workspace
        tasks:
          - id: W1.T1
            task: Create Cargo workspace `crates/zbrain` (bin crate, module stubs mirroring internal/runtime files: paths, config, workspace, coordination), rust-toolchain.toml pinned to stable, `.gitignore` entries for `target/`.
            depends_on: none
            expected_output: `cargo build` green; `cargo clippy -D warnings` green; empty main runs.
          - id: W1.T2
            task: Port `internal/runtime/paths.go` (ResolvePaths/EnsureConfig/Options), file-mode enforcement (0700 dirs, 0600 mutable, 0400 evidence, 0600 derived) and `workspace.go` (CreateWorkspace) with unit tests mirroring Go cases.
            depends_on: W1.T1
            expected_output: Path resolution and mode bits byte-equivalent with Go fixtures; chmod tests assert every mode.
          - id: W1.T3
            task: Port `coordination.go` flock (exclusive/shared acquireWorkspaceLock, CoordinationLockPath) via `libc::flock`, plus the injectable Clock trait (chrono) replacing Go's `Now func()`.
            depends_on: W1.T2
            expected_output: Lock contention test (two processes, exclusive blocks, shared coexists) passes; cross-process test like Go's.
      - wave: W2
        goal: Fixture oracle plumbing
        tasks:
          - id: W2.T1
            task: Build `crates/tools/fixture-gen` (Go program importing `internal/runtime`) that creates a fixture workspace and emits deterministic JSON/digest/tree fixtures into `crates/zbrain/tests/fixtures/`; add `scripts/parity.sh` running a command against both binaries and diffing tolerant JSON.
            depends_on: W1.T3
            expected_output: `go run ./crates/tools/fixture-gen` (or `go test`-driven) writes fixtures; parity.sh reports schema-level diff for a trivial command (setup+status).
    checks:
      - command: cargo test -p zbrain --lib
        expects: All m0 unit tests pass (paths, modes, flock, clock).
      - command: cargo clippy --workspace -- -D warnings
        expects: Clean.
      - command: scripts/parity.sh status
        expects: Go vs Rust setup/status JSON schema-identical on a fresh ZBRAIN_HOME fixture.
    stop_condition: flock semantics cannot be matched cross-platform on macOS+Linux via libc — stop and report (foundation invalidates everything).
    escalation: Owner; do not substitute advisory locks with lock files.

  - phase_slug: m1-assets-setup
    story_id: rust-m1-assets-setup-20260904
    status: planned
    goal: Embedded assets and `zbrain setup` ported byte-identical
    depends_on: m0-foundation
    requirements: [R2, R8]
    allowed_surfaces: [crates/zbrain/src/setup.rs, crates/zbrain/src/assets.rs, crates/zbrain/tests/]
    avoided_surfaces: [assets/ (read-only), internal/cli/cli.go]
    waves:
      - wave: W1
        goal: Embed and extract assets
        tasks:
          - id: W1.T1
            task: Embed `assets/` via `include_dir` (README.md, agents/, engine/, skills/, templates/, workspaces/.gitkeep) and port the setup extraction (copy rules, existing-file skip with assets_copied/skipped counts).
            depends_on: none
            expected_output: Extracted tree byte-identical to Go setup output (golden tree fixture).
          - id: W1.T2
            task: Port setup JSON output (schema_version, config_created, assets_copied, assets_skipped) and CLI arg handling for `setup` (thin dispatch stub that M7 replaces wholesale).
            depends_on: W1.T1
            expected_output: parity.sh setup fixture match.
    checks:
      - command: cargo test -p zbrain --lib setup
        expects: Extraction/counts tests pass.
      - command: scripts/parity.sh setup
        expects: Diff clean vs Go.
    stop_condition: include_dir cannot represent a required asset layout (e.g. empty dir workspaces/.gitkeep) — stop and report.
    escalation: Owner; do not change the extracted tree shape.

  - phase_slug: m2-claims-evidence
    story_id: rust-m2-claims-evidence-20260904
    status: planned
    goal: Claim store, evidence store (SHA-256 skip, digest, immutability), manifest ported with two-way tree readability
    depends_on: m1-assets-setup
    requirements: [R2, R3, R7]
    allowed_surfaces: [crates/zbrain/src/claims.rs, crates/zbrain/src/evidence.rs, crates/zbrain/src/manifest.rs, crates/zbrain/src/yaml.rs, crates/zbrain/tests/fixtures/]
    avoided_surfaces: [internal/runtime/claim*.go, internal/runtime/evidence.go (read-only oracle)]
    waves:
      - wave: W1
        goal: YAML/OKF frontmatter layer
        tasks:
          - id: W1.T1
            task: Port claim YAML marshal/unmarshal on serde_yml configured to emit yaml.v3-compatible block style (field order preserved via struct order); property tests round-trip every fixture source.yaml and claim Markdown frontmatter.
            depends_on: none
            expected_output: All Go fixtures round-trip byte-identical (modulo nothing — order is deterministic).
      - wave: W2
        goal: Evidence store
        tasks:
          - id: W2.T1
            task: Port evidence.go: AddFile (write-once 0400 raw + source.yaml), SHA-256 duplicate skip (Deduped true, no dirty, no generation bump), Read/Verify (tamper, size, hash, metadata checks), EvidenceValidator cache, workspace boundary + symlink escape rejection.
            depends_on: W1.T1
            expected_output: 1:1 port of evidence_test.go cases (immutability is trust-critical, R7); golden fixtures match.
      - wave: W3
        goal: Claim store and manifest
        tasks:
          - id: W3.T1
            task: Port claim.go + claim_store.go: WriteDraft, draft-time contradiction detection surface, RenderClaimMarkdown digest, ScanWorkspaceForTrust, manifest.go write-once + dirty barrier semantics (canonical tree unchanged on dirty failure).
            depends_on: W2.T1
            expected_output: Golden claim fixtures match; Go binary reads a Rust-written workspace tree and reports identical status/digest (differential proof), and vice versa.
    checks:
      - command: cargo test -p zbrain --lib evidence
        expects: Full evidence test set passes including tamper/dedup/dirty-barrier.
      - command: cargo test -p zbrain --lib claims
        expects: Draft/render/scan tests pass.
      - command: scripts/parity.sh claims-evidence
        expects: Two-way tree readability proof clean.
    stop_condition: serde_yml cannot reproduce yaml.v3 output ordering needed for byte-identical source.yaml — stop and report (R2 at risk).
    escalation: Owner; do not switch YAML emitter silently.

  - phase_slug: m3-lifecycle-trust
    story_id: rust-m3-lifecycle-trust-20260904
    status: planned
    goal: Lifecycle transitions, trust validation, doctor lint ported 1:1
    depends_on: m2-claims-evidence
    requirements: [R3, R7]
    allowed_surfaces: [crates/zbrain/src/lifecycle.rs, crates/zbrain/src/trust.rs, crates/zbrain/src/lint.rs, crates/zbrain/src/transition.rs]
    avoided_surfaces: [internal/runtime/lifecycle.go, internal/runtime/trust_validation.go (read-only oracle)]
    waves:
      - wave: W1
        goal: Lifecycle and trust
        tasks:
          - id: W1.T1
            task: Port lifecycle.go (approve with verified.at/by/digest, supersede chain, revoke) and transition.go journaling.
            depends_on: none
            expected_output: 1:1 lifecycle test port passes; supersede/approve digests match Go fixtures.
          - id: W1.T2
            task: Port trust_validation.go (approved+digest+closure validation, promotion_candidates, blocked/gap fail-closed) and evidence-rules from query trust path (read-only port of the validator).
            depends_on: W1.T1
            expected_output: 1:1 trust test port passes; blocked/gap fixtures match.
      - wave: W2
        goal: Lint and drift
        tasks:
          - id: W2.T1
            task: Port lint.go StructuralFindings (dangling refs, orphan evidence, duplicate SHA, stale_after) and evidence CheckDrift (unchanged/changed/missing/uncheckable + affected claims).
            depends_on: W1.T2
            expected_output: Drift fixtures from docs/proofs reproduced identically.
    checks:
      - command: cargo test -p zbrain --lib lifecycle
        expects: Full lifecycle/trust/lint suites pass.
      - command: scripts/parity.sh doctor
        expects: Doctor JSON (findings, exit semantics) matches Go oracle.
    stop_condition: Digest computation (RenderClaimMarkdown) cannot be reproduced byte-identically — stop and report (trust contract broken).
    escalation: Owner; digest format is sacred.

  - phase_slug: m4-index-query
    story_id: rust-m4-index-query-20260904
    status: planned
    goal: FTS5 index, query/ask, temporal recall, loopback embedder ported; perf at or better than Go
    depends_on: m3-lifecycle-trust
    requirements: [R5, R7, R8]
    allowed_surfaces: [crates/zbrain/src/index.rs, crates/zbrain/src/query.rs, crates/zbrain/src/search.rs, crates/zbrain/src/embedder.rs, crates/zbrain/benches/]
    avoided_surfaces: [internal/runtime/index.go (read-only oracle), indexes/]
    waves:
      - wave: W1
        goal: Index store on rusqlite
        tasks:
          - id: W1.T1
            task: Assert FTS5 availability in bundled build (compile-time + runtime test on a temp db); port index.go schema DDL, Rebuild, CheckFresh, dirty marker, generation/changeToken, claim catalog fill.
            depends_on: none
            expected_output: FTS5 assert green; index db byte-compatible enough that a Go-produced index opens in Rust and vice versa.
          - id: W1.T2
            task: Port search.go (FTS query building, tokenization parity with Go unicode rules) and index_state.go.
            depends_on: W1.T1
            expected_output: Ranked result fixtures match Go on the eval query set (docs/eval/queries.json lexical tracks).
      - wave: W2
        goal: Query, temporal, embedder
        tasks:
          - id: W2.T1
            task: Port query.go (ask fail-closed semantics: missing/dirty/stale/rejected, workspace --include read-only), temporal filtering (after/before/as-of), contradiction surfacing.
            depends_on: W1.T2
            expected_output: Golden ask fixtures byte-identical on deterministic fields across the eval query corpus.
          - id: W2.T2
            task: Port embedder.go loopback token embedder + hybrid ranking (RRF as implemented); verify hybrid fixtures match Go.
            depends_on: W2.T1
            expected_output: Hybrid eval track matches; no network calls.
          - id: W2.T3
            task: Port p95@100K bench as a cargo bench mirroring TestAskP95At100K; record into docs/proofs/rust-bench-100k.json.
            depends_on: W2.T2
            expected_output: p95 ≤ Go baseline from docs/proofs/bench-baseline-full.json (same box, same fixture).
    checks:
      - command: cargo test -p zbrain --lib index
        expects: Index/freshness/dirty/catalog tests pass.
      - command: cargo test -p zbrain --lib query
        expects: Fail-closed + temporal + hybrid fixtures pass.
      - command: cargo bench -p zbrain -- p95_100k
        expects: p95 ≤ Go baseline; JSON proof written.
    stop_condition: FTS5 unavailable in bundled build after fallback attempt — stop (premise collapse; escalate to owner with both build paths tried).
    escalation: Owner; do not swap SQLite engine.
    notes: Bench box variance acknowledged — compare on the same box in the same session as the Go baseline run.

  - phase_slug: m5-mcp-gateway
    story_id: rust-m5-mcp-gateway-20260904
    status: planned
    goal: Hand-rolled stdio MCP gateway with current protocol revisions and conformance suite
    depends_on: m4-index-query
    requirements: [R6, R7]
    allowed_surfaces: [crates/zbrain/src/mcp/ (mod, jsonrpc, tools, resources, transport traits), crates/zbrain/tests/mcp/]
    avoided_surfaces: [internal/mcp/ (read-only oracle), assets/engine (no wording changes)]
    waves:
      - wave: W1
        goal: Transport and framing
        tasks:
          - id: W1.T1
            task: Implement JSON-RPC 2.0 stdio framing (serde_json) with IO traits (Read/Write) so tests drive in-memory transports like Go; safeWriter-equivalent mutex for stderr diagnostics; stdout carries only protocol frames.
            depends_on: none
            expected_output: Framing tests pass (batch absent? match current behavior; oversized/unknown → -32602; server faults → -32603).
          - id: W1.T2
            task: Implement protocol revision handling exactly as current: legacy handshake revisions and stateless `server/discover` + per-request `_meta`; schema-invalid tool input → isError result.
            depends_on: W1.T1
            expected_output: Revision tests (ported from mcp_test.go) pass.
      - wave: W2
        goal: Tools and resources
        tasks:
          - id: W2.T1
            task: Port the 10 tools (memory_ask, memory_status, evidence_capture, evidence_check, claim_lifecycle prepare/apply, campaign_begin/next/submit_draft, doctor-equivalent) and 2 resources (claim read, fenced untrusted_evidence envelope with nested raw bytes).
            depends_on: W1.T2
            expected_output: Tool/response fixtures match Go; resource fence shape byte-identical.
          - id: W2.T2
            task: Real-client smoke: run the gateway against Claude Code and Codex MCP configs in isolated ZBRAIN_HOME; capture transcript proofs.
            depends_on: W2.T1
            expected_output: Both clients list tools, read resources, call memory_ask successfully.
    checks:
      - command: cargo test -p zbrain --lib mcp
        expects: Conformance suite passes.
      - command: scripts/parity.sh mcp-stdio
        expects: scripted protocol exchange diffs clean vs Go gateway.
    stop_condition: A required protocol behavior cannot be expressed without the Go SDK's session machinery — stop and report; do not silently relax error-code semantics.
    escalation: Owner; protocol contract is gateway-spec-bound.

  - phase_slug: m6-approval-campaign
    story_id: rust-m6-approval-campaign-20260904
    status: planned
    goal: Owner-pinned approval ceremony, batch approval, and authoring campaign ported 1:1
    depends_on: m5-mcp-gateway
    requirements: [R3, R7]
    allowed_surfaces: [crates/zbrain/src/approval.rs, crates/zbrain/src/campaign.rs, crates/zbrain/src/term.rs]
    avoided_surfaces: [internal/runtime/challenge.go (read-only oracle)]
    waves:
      - wave: W1
        goal: Challenge and single grant
        tasks:
          - id: W1.T1
            task: Port challenge.go single-path (prepare/show/grant/apply, 15m challenge, 5m token capped by challenge, single-use, TTY confirm of last-16-hex digest via `console`).
            depends_on: none
            expected_output: 1:1 challenge test port passes (timing, single-use, digest confirm).
      - wave: W2
        goal: Batch and campaign
        tasks:
          - id: W2.T1
            task: Port batch approval (one challenge binds N draft digests, per-item confirm/skip, per-item applied/skipped/failed).
            depends_on: W1.T1
            expected_output: Batch test port passes; fixtures match.
          - id: W2.T2
            task: Port campaign.go (campaign_begin/next/submit_draft, resumable run file, drafts-only, no auto-approve) and the MCP campaign tools' runtime side.
            depends_on: W2.T1
            expected_output: Campaign test port passes; R3 negative proof (campaign cannot approve) reproduced.
    checks:
      - command: cargo test -p zbrain --lib approval
        expects: Challenge/batch/campaign suites pass.
      - command: scripts/parity.sh approval
        expects: prepare/show JSON matches Go oracle; grant path exercised with a pty-emulated TTY test.
    stop_condition: TTY confirm cannot be driven deterministically in tests on macOS+Linux — stop and report.
    escalation: Owner; ceremony semantics are non-negotiable.

  - phase_slug: m7-cli-view-eval
    story_id: rust-m7-cli-view-eval-20260904
    status: planned
    goal: Full CLI surface, loopback viewer, eval suite, and smoke script ported; differential harness covers every command
    depends_on: m6-approval-campaign
    requirements: [R3, R4, R7]
    allowed_surfaces: [crates/zbrain/src/cli.rs, crates/zbrain/src/view.rs, crates/eval/ (or tests/eval), scripts/smoke.sh, Makefile.smoke]
    avoided_surfaces: [internal/cli/, internal/view/, internal/eval/ (read-only oracle)]
    waves:
      - wave: W1
        goal: CLI dispatch and output
        tasks:
          - id: W1.T1
            task: Port arg parsing/dispatch, help texts, JSON/text writers, exit-code semantics (2 on domain findings) for every command: setup, workspace, evidence, claim, migrate, reindex, ask, status, doctor, mcp, view, approval.
            depends_on: none
            expected_output: --help output byte-identical to Go; exit codes match on fixture sets.
      - wave: W2
        goal: Viewer and eval
        tasks:
          - id: W2.T1
            task: Port view server on std::net TcpListener + threads (GET/HEAD only, 127.0.0.1 bind, CSP/nosniff headers, no CORS) with static assets embedded.
            depends_on: W1.T1
            expected_output: Header/security tests pass; page renders fixture workspace.
          - id: W2.T2
            task: Port internal/eval six-track suite as cargo tests (trust-integrity, draft-precision, lifecycle, retrieval, drift, perf+race equivalents); port make smoke as scripts/smoke.sh in isolated ZBRAIN_HOME.
            depends_on: W2.T1
            expected_output: Eval tracks produce metrics matching docs/proofs baselines within documented tolerance; smoke rc=0.
    checks:
      - command: cargo test -p zbrain --lib cli
        expects: Dispatch/exit-code/help tests pass.
      - command: scripts/parity.sh all
        expects: Full differential run over the command surface diffs clean.
      - command: scripts/smoke.sh
        expects: rc=0 in isolated ZBRAIN_HOME.
    stop_condition: A CLI output contract (help text, JSON schema) cannot be matched — stop and report; help text is user-visible contract.
    escalation: Owner; do not "improve" help wording during migration.

  - phase_slug: m8-cutover
    story_id: rust-m8-cutover-20260904
    status: planned
    goal: Single cutover PR: CI→Rust, docs/authority→Rust, delete Go tree
    depends_on: m7-cli-view-eval
    requirements: [R1, R4, R9, R10]
    allowed_surfaces: [.github/workflows/test.yml, Makefile, AGENTS.md, README.md, docs/, trusted-memory-spec.md, docs/trusted-agent-gateway-spec.md, internal/, cmd/ (deleted), go.mod (deleted), assets/ (only if Go-specific references found)]
    avoided_surfaces: [crates/ (frozen at cutover except CI-driven fixes)]
    waves:
      - wave: W1
        goal: Authority flip
        tasks:
          - id: W1.T1
            task: Rewrite `.github/workflows/test.yml` (dtolnay/rust-toolchain@stable; order: cargo test → clippy -D warnings → cargo build --release → smoke → git diff --check; ubuntu+macos matrix), Makefile (build → cargo build --release + strip, smoke → scripts/smoke.sh), and update AGENTS.md commands/layout/authority Go→Rust.
            depends_on: none
            expected_output: CI green on the branch with Rust-only gates.
          - id: W1.T2
            task: Repo-wide sweep for Go references (docs/, README.md, specs, assets/): update command/test sections; delete internal/, cmd/, go.mod, go.sum; verify no second initiative markdown and plans reflect Rust authority.
            depends_on: W1.T1
            expected_output: `rg -i "golang|go test|go build" --glob '!dist/**'` returns only intentional historical references in completed plans.
          - id: W1.T3
            task: Cutover PR to master; tag pre-cutover Go release; verify revert path on a scratch branch.
            depends_on: W1.T2
            expected_output: Merge; `git revert` on scratch branch restores Go tree and its CI passes.
    checks:
      - command: cargo test --workspace && cargo clippy --workspace -- -D warnings
        expects: Green on master post-merge.
      - command: make build && make smoke
        expects: Rust build + smoke rc=0 on master.
      - command: git diff --check
        expects: Clean.
    stop_condition: Any trust-critical differential still failing at cutover review — cutover halts; Go stays authoritative.
    escalation: Owner; cutover is irreversible-by-default (revert is the only path back).

## Progress
<!-- Append-only durable entries record timestamp, phase, wave, task, task_status, run_id, trace_id, exact verification/result, and changed surfaces or blocker. -->
- none

## Decisions
<!-- Append-only durable entries record timestamp, phase/task, decision, and rationale. -->
- 2026-09-04: Big-bang over strangler/hybrid; Go binary retained as living parity oracle until cutover.
- 2026-09-04: rusqlite bundled over Turso/Limbo (unproven FTS5); hand-rolled MCP JSON-RPC over rmcp (custom protocol revisions).
- 2026-09-04: Workspace lives in-repo under `crates/` on branch `rust-rewrite` (history/asset adjacency; single-repo single-identity).
- 2026-09-04: No async runtime; concurrency surface (3 files) ports with std threads.

## Validation
<!-- Append-only durable entries record timestamp, phase, exact command/result/output, run_id, check_id, verdict, and proof_gaps. -->
- none

## Current State and Next Action
- active_phase: none (all phases planned; none started)
- lifecycle_status: planned
- latest_run_id: none
- latest_trace_ids: []
- latest_check_id: none
- latest_handoff_id: none
- blockers: none
- open_items: []
- exact_next_action: work full — start with m0-foundation

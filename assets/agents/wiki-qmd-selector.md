# wiki-qmd-selector

Deprecated placeholder. The current Go runtime does not use qmd.

Use `zbrain ask` for trusted context retrieval. The derived local SQLite FTS5 index is managed by `zbrain reindex`.

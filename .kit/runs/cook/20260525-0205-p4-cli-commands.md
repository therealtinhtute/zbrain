# Cook Run: p4-cli-commands

Mode: full
Phase: p4-cli-commands
Started At: 2026-05-25 02:05
Status: done

## Preflight
- verdict: ready
- artifact check: `.kit/planning/SPEC.md`, `.kit/planning/ROADMAP.md`, `p4-cli-commands` context, and plan exist
- contract drift: none detected before implementation

## Scope Confirmation
- phase goal: make the four public CLI commands work end to end against a temp runtime and project
- wave execution:
  - T1 `zwiki setup`
  - T2 `zwiki workspace create`
  - T3 `zwiki update`
  - T4 `zwiki init`

## Task Status
### T1 — Implement `zwiki setup`
- status: DONE
- evidence:
  - extracts runtime assets into `~/.zwiki/`
  - creates `config.yml` when missing
  - reports qmd status through the clack-driven UI
- verification:
  - `bun test --run tests/commands.integration.test.ts`

### T2 — Implement `zwiki workspace create`
- status: DONE
- evidence:
  - validates workspace names
  - scaffolds the workspace directory tree and starter files
  - sets `default_workspace` when it was previously unset
- verification:
  - `bun test --run tests/commands.integration.test.ts`

### T3 — Implement `zwiki update`
- status: DONE
- evidence:
  - refreshes bundled runtime assets
  - preserves user-authored workspace content
- verification:
  - `bun test --run tests/commands.integration.test.ts`

### T4 — Implement `zwiki init`
- status: DONE
- evidence:
  - writes `.claude/zwiki.json`
  - links command and agent assets into the current project
  - appends zwiki rules to `CLAUDE.md` non-destructively
  - writes qmd MCP settings when requested
- verification:
  - `bun test --run tests/commands.integration.test.ts`
  - `bun test --run`
  - `bunx tsc --noEmit`
  - `bun run build.ts`
  - `bun run src/index.ts --help`
